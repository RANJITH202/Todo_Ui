import { StartEndDatePipe } from './start-end-date.pipe';

describe('StartEndDatePipe', () => {
  it('create an instance', () => {
    const pipe = new StartEndDatePipe();
    expect(pipe).toBeTruthy();
  });
});
